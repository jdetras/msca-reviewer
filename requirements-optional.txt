# Optional extras. The core panel, the Word/.docx parser and the web UI need
# nothing beyond the Python standard library. Install this only if you want to
# review proposals supplied as PDF.
pypdf>=4.0          # PDF text extraction (alternative: pdfminer.six)
